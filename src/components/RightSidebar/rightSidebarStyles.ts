import { makeStyles } from '@material-ui/core/styles';

export const useRightSidebarStyles = makeStyles(() => ({
    root: {
        position: 'sticky',
        top: 5,
        paddingLeft: 20,
    },
}));
